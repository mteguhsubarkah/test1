version: '2'
services:
  soundwav-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/soundWav/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=soundWav
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
